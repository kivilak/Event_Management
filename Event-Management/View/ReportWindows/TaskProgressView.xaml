<UserControl x:Class="Event_Management.View.ReportWindows.TaskProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Event_Management.View.ReportWindows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ReportStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text=" Task Progress Report" FontSize="24" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBlock Text="Task completion status by category" FontSize="14" Margin="0 0 0 20"/>
            <DataGrid x:Name="progressGrid"
                      AutoGenerateColumns="False"
                      Style="{StaticResource EventsDataGrid}"
                      RowStyle="{StaticResource SelectableRowStyle}"
                      CellStyle="{StaticResource PlainDataGridCellStyle}"
                      CanUserAddRows="False"
                      CanUserResizeRows="False"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column" 
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="2*"/>
                    <DataGridTextColumn Header="Completed" Binding="{Binding Completed}" Width="2*"/>
                    <DataGridTextColumn Header="Pending" Binding="{Binding Pending}" Width="2*"/>
                    <DataGridTextColumn Header="Overdue" Binding="{Binding Overdue}" Width="2*"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="2*"/>
                    <DataGridTextColumn Header="Completion Rate" Binding="{Binding CompletionRate}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
