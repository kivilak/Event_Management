<UserControl x:Class="Event_Management.View.ReportWindows.EventSummary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Event_Management.View.ReportWindows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="📊 Event Summary Report" FontSize="24" FontWeight="Bold" Margin="0 0 0 10" Foreground="#1F2937"/>
            <TextBlock Text="Overview of all events with key metrics" FontSize="14" Margin="0 0 0 20" Foreground="#6B7280"/>
            <DataGrid x:Name="eventGrid" AutoGenerateColumns="False" HeadersVisibility="Column" IsReadOnly="True" GridLinesVisibility="None"
                  RowBackground="#F9FBFE" AlternatingRowBackground="#EFF4FF" BorderBrush="#D1D5DB" BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Event Name" Binding="{Binding EventName}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                    <DataGridTextColumn Header="Guests" Binding="{Binding Guests}" Width="*"/>
                    <DataGridTextColumn Header="Budget" Binding="{Binding Budget}" Width="*"/>
                    <DataGridTextColumn Header="Spent" Binding="{Binding Spent}" Width="*"/>
                    <DataGridTextColumn Header="Remaining" Binding="{Binding Remaining}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
