<UserControl x:Class="Event_Management.View.ReportWindows.TaskProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Event_Management.View.ReportWindows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="\u2197 Task Progress Report" FontSize="24" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBlock Text="Task completion status by category" FontSize="14" Margin="0 0 0 20"/>
            <DataGrid x:Name="progressGrid" AutoGenerateColumns="False" HeadersVisibility="Column" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                    <DataGridTextColumn Header="Completed" Binding="{Binding Completed}" Width="*"/>
                    <DataGridTextColumn Header="Pending" Binding="{Binding Pending}" Width="*"/>
                    <DataGridTextColumn Header="Overdue" Binding="{Binding Overdue}" Width="*"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="*"/>
                    <DataGridTextColumn Header="Completion Rate" Binding="{Binding CompletionRate}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
